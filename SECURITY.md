# Security Policy

## 支持状态

`v1.0.0` 正在稳定化，尚未发布。当前安全维护范围为：

| 版本                           | 状态                                                           |
| ------------------------------ | -------------------------------------------------------------- |
| `v0.13.10`                     | 稳定化期间尽力修复高影响漏洞，并保留到 `v1.0.0` 的直接升级路径 |
| `v0.13.9`                      | 不完整历史发布，不提供安全补丁                                 |
| `v0.13.8` 及更早               | 不提供安全补丁，应先升级到 `v0.13.10`                          |
| `edge`、`sha-*`、alpha/beta/RC | 仅用于验证，不属于稳定安全支持                                 |

`v1.0.0` 完整发布后，稳定版本按 [SUPPORT.md](./SUPPORT.md) 的 Active/Security 生命周期获得修复；`v0.13.10` 进入 30 天迁移窗口后结束维护。发布说明可以因已知漏洞缩短某个版本的迁移期限，但不会把预发布通道标记为稳定。

## 私密报告漏洞

使用 GitHub Private vulnerability reporting / Security Advisory：

<https://github.com/Xwordsman/nextbuf/security/advisories/new>

不要在公开 Issue、Discussion、社区帖子或聊天中提交可利用细节、真实密钥、用户数据或未修复漏洞的完整复现。若私密报告入口出现平台故障，只提交不含利用细节的公开 Issue，请维护者建立私密联系渠道。

报告建议包含：

- 受影响的精确版本、commit、镜像 Digest 和 CPU 架构。
- 漏洞类型、影响范围、攻击者所需权限和前置条件。
- 最小复现步骤或概念验证，以及是否接触真实数据。
- Web/Worker/CLI/迁移/发布链路中受影响的组件。
- 已测试的缓解方式或修复建议（如有）。

发送前删除 Cookie、Session、密码、Token、连接串、邮件载荷、对象存储密钥、真实用户内容和可识别个人信息。需要样本数据时使用合成记录。

## 范围

以下问题属于安全报告范围：

- 认证、会话、邮箱验证、密码重置或 OAuth 绕过。
- 越权读取/修改私人草稿、隐藏内容、附件、后台数据或治理记录。
- 注入、服务端请求伪造、路径穿越、不安全文件处理、XSS 或 CSRF。
- 密钥、日志、备份、邮件载荷或 Provider 凭据泄露。
- 制裁、管理员连续性、setup、迁移、备份恢复和 Worker 幂等性的安全破坏。
- 官方镜像、Release 归档、依赖或 GitHub Actions 供应链完整性问题。

纯功能建议、自行删除许可署名、第三方主机/SMTP/S3 服务故障、非官方镜像、私有改包和已结束支持版本的一般缺陷不按私密漏洞流程处理；其中若能证明会影响当前受支持的官方产物，报告时说明跨版本影响。

## 响应与披露

维护目标如下；它们是社区项目的响应目标，不是 SLA：

- 3 个工作日内确认收到。
- 7 个工作日内完成初步分级或请求补充材料。
- 修复协调期间至少每 14 天更新一次状态。

维护者会验证影响、确定受影响版本、准备测试和发布说明，并与报告者协调披露时间。修复优先进入当前 Active patch；仍在 Security 窗口的版本只回移高/严重问题。公开公告在修复产物和可执行缓解措施就绪后发布，并尽可能致谢报告者；报告者要求匿名时不公开身份。

已知正在被利用或可能造成大规模数据损坏的问题会优先处理。部署者仍负责限制数据库/Redis 公网访问、保护秘密、验证备份、及时升级并监控异常；这些运维责任不改变应用漏洞的受理范围。
