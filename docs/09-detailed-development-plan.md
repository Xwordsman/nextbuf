# 详细开发计划

本文把 NextBuf 从项目初始化到长期平台化拆成按功能交付的版本。它是产品、开发、测试和发布共同使用的执行基线，不是不可调整的日期承诺。

## 1. 版本规划原则

### 1.1 为什么项目骨架不叫 v1.0.0

`v1.0.0` 按语义化版本应代表首个可以公开部署、具有升级承诺的稳定版本。如果把只有项目骨架的代码发布为 `v1.0.0`，后续注册登录、发帖等正常功能就只能错误地塞进补丁版本，版本号会失去意义。

因此采用以下规则：

- `v0.x.0`：正式版之前的功能里程碑，可以发生较大内部变化。
- `v1.0.0-alpha.N`：核心链路尚未完整，只供开发测试。
- `v1.0.0-beta.N`：功能基本冻结，集中修复和验证升级。
- `v1.0.0-rc.N`：发布候选，只接受阻断发布的问题修复。
- `v1.0.0`：首个可运营、可升级、可恢复的稳定社区版本。
- `v1.0.1`：兼容性错误、安全或文档修复，不加入大型新功能。
- `v1.1.0`：向后兼容的新功能。
- `v2.0.0`：存在明确破坏性变更或进入新的平台能力阶段。

开发里程碑可以发布镜像，但 `v0.x` 必须标注为预览版，不承诺无损升级到所有后续预览版本。进入 Beta 前必须提供到 `v1.0.0` 的迁移路径。

### 1.2 版本不是分支

每个版本是一组可验收能力，不为每个长期保留独立开发分支。默认流程：

```text
Issue/Epic -> 功能分支 -> Pull Request -> main -> 版本标签
```

未完成的大功能使用功能开关隐藏，不能让主分支长期不可运行。

### 1.3 版本状态

- **Committed**：已进入近期开发范围，功能和验收标准应明确。
- **Planned**：方向明确，细节在进入开发前再次评审。
- **Exploratory**：长期能力地图，不构成发布时间或兼容承诺。

`v0.1.0` 至 `v1.0.0` 为 Committed 规划；`v1.1.0` 至 `v3.x` 为 Planned；`v4.x` 至 `v10.x` 为 Exploratory。

## 2. 总体版本地图

| 系列 | 主题 | 最终交付结果 | 状态 |
| --- | --- | --- | --- |
| `v0.1` - `v0.3` | 工程、运行时、设计系统 | 可持续开发的基础工程 | Committed |
| `v0.4` - `v0.8` | 身份、内容、互动、发现 | 完整的用户讨论主链路 | Committed |
| `v0.9` - `v0.11` | 通知、治理、信任、后台 | 社区具备运营能力 | Committed |
| `v0.12` - `v1.0` | 部署、加固、稳定发布 | 可公开自托管的 V1 | Committed |
| `v1.1` - `v1.5` | V1 增强 | 提升发现、运营和可维护性 | Planned |
| `v2.x` | 开放 API、专业身份、服务场景 | 社区成为可集成的服务平台 | Planned |
| `v3.x` | 插件平台 | 第三方可按稳定协议扩展 | Planned |
| `v4.x` | 交易与支付 | 可审计的订单、支付和争议系统 | Exploratory |
| `v5.x` | 主题和扩展生态 | 可分发、可验证的扩展市场 | Exploratory |
| `v6.x` | 高可用和规模化 | 支持多实例和大规模运营 | Exploratory |
| `v7.x` | 组织与多社区 | 一个部署承载多个自治空间 | Exploratory |
| `v8.x` | 联邦与数据可携带 | 受控的跨社区协作 | Exploratory |
| `v9.x` | AI 原生能力 | 可控、透明的 AI 辅助社区 | Exploratory |
| `v10.x` | LTS 与生态成熟 | 稳定平台、迁移体系和长期支持 | Exploratory |

## 3. V1 前置开发：从空仓库到稳定版

### v0.1.0：项目骨架

状态：**Completed（2026-07-12）**

目标：建立所有后续功能共同依赖的仓库、构建和代码质量基础。

开发内容：

- 初始化 Next.js 16.2.10、TypeScript 严格模式、App Router 和 pnpm lockfile。
- 建立 `src/app/`、`src/components/`、`src/modules/`、`src/infrastructure/`、`src/worker/`、`tests/`、`deploy/` 目录。
- 配置 `server-only`、`*.server.ts` 和导入边界规则，阻止 Client Component 引入数据库、Redis、队列或密钥代码。
- 配置 ESLint、Prettier、TypeScript 检查和统一导入别名。
- 建立环境变量 Schema、开发/测试/生产配置入口。
- 建立结构化日志、请求 ID、统一错误类型和错误响应格式。
- 建立基础 CI：安装、格式检查、lint、类型检查、单元测试、构建。
- 添加标准 AGPLv3 `LICENSE`、第 7(b) 条附加署名 `NOTICE`、许可证说明、贡献指南、Issue 和 Pull Request 模板。
- 建立版本信息页面或 CLI，使运行实例能报告版本、提交和构建时间。

不包含：数据库业务模型、注册、发帖和正式页面。

验收门槛：

- 新环境按照 README 可以完成安装和 `pnpm dev`。
- `pnpm build`、类型检查和测试在 Node.js 24 LTS 下通过。
- CI 对主分支和 Pull Request 生效。
- 空首页、错误页和健康检查入口可以访问。
- `package.json`、LICENSE、NOTICE、README 和贡献条款对许可证的描述一致。

### v0.2.0：数据库、缓存、队列和运行角色

状态：**Next**

目标：打通 Web、Worker、PostgreSQL、Redis 和迁移基础设施。

开发内容：

- 接入 PostgreSQL 18 和 Prisma，建立初始迁移与数据库连接管理。
- 接入 Redis 8，划分缓存、限流和 BullMQ key 前缀。
- 建立 `web`、`worker`、`migrate`、`setup`、`doctor` 启动入口。
- 建立 BullMQ 队列注册、任务版本、重试、超时和死信基础结构。
- 建立 Outbox 表、Dispatcher 骨架和幂等任务辅助工具。
- 实现 `/health/live`、`/health/ready` 及 Worker 健康状态。
- 提供开发环境 Compose，验证 PostgreSQL 18 数据卷路径。
- 建立测试数据库隔离、迁移测试和 Redis 集成测试工具。

验收门槛：

- 空库能通过 `setup` 初始化，重复执行不会破坏数据。
- Web 可写入一条测试事务，Outbox 可投递到 Worker 并完成幂等消费。
- 单独停止 Worker 不影响 Web 健康；恢复后能继续处理任务。
- 清空 Redis 后业务事实数据仍完整，缓存可以重建。

### v0.3.0：设计系统与页面框架

目标：把 `UI/index.html` 转换为可复用、响应式、可访问的真实应用外壳。

开发内容：

- 建立 Tailwind、shadcn/ui 和颜色、排版、间距、边框、阴影 token。
- 实现顶部导航、三栏桌面框架、移动端导航和底部信息区。
- 在全局页脚实现不可配置关闭的 `Powered by NextBuf` 官方链接，并覆盖认证、错误和后台等特殊布局。
- 实现主题列表项、节点标识、状态标识、侧栏模块、空状态和分页骨架。
- 实现头像按钮及用户菜单的匿名/登录占位状态。
- 建立按钮、输入框、下拉菜单、弹窗、提示、表格、Tabs 和表单组件。
- 建立加载、错误、无权限、无数据和维护状态页面。
- 配置基本无障碍检查和 Playwright 多视口截图测试。
- 保持既定内容宽度，不因风格修改改变三栏布局尺寸。

验收门槛：

- 首页原型拆分为组件，不再依赖一份巨型静态 HTML。
- 桌面和移动端无重叠、横向溢出和不可点击控件。
- 键盘可操作导航、菜单和弹窗，焦点状态清晰。
- UI 组件不包含伪造的后端业务规则。

### v0.4.0：注册、登录和账号安全

目标：建立可用于生产的身份认证主链路。

开发内容：

- 选择并记录认证库 ADR，完成账号、凭证、会话和验证令牌模型。
- 实现邮箱密码注册、邮箱验证、登录和退出。
- 实现忘记密码、重置密码和全部设备退出。
- 实现安全 Cookie、会话过期、撤销和设备列表。
- 实现登录、注册、验证、重置的限流和防账号枚举。
- 建立 OAuth Provider 接口，首个 Provider 根据决策启用。
- 实现注册策略：开放、邀请制、关闭注册。
- 记录关键身份安全审计事件。

验收门槛：

- 注册到邮箱验证再到登录的 E2E 通过。
- 重置密码后旧会话按策略失效。
- 未登录用户无法访问受保护接口，前端隐藏不是唯一防线。
- Token 数据库只保存哈希，日志不出现密码、Cookie 或验证码。

### v0.5.0：用户资料与账号中心

目标：完整实现 UID、用户名、昵称和公开用户页面。

开发内容：

- 注册时分配不可变数字 UID。
- 实现唯一 `@username`、可重复昵称和用户名规范化规则。
- 实现用户名保留词、修改冷却期和历史别名表。
- 实现头像上传、裁剪、默认头像和对象存储接口。
- 实现简介、主页、注册时间、活动统计等公开资料。
- 实现账号中心：资料、安全、会话、通知偏好占位和隐私设置。
- 实现公开用户页、头像下拉菜单和用户名跳转。
- 实现用户软删除/注销的第一阶段流程。

验收门槛：

- UID 不可修改、不复用；用户名规范化后有数据库唯一约束。
- 用户名变更后旧链接按策略跳转，不能立即被冒用。
- 头像上传经过类型、大小和权限校验。
- 用户菜单按设计显示昵称、`@username`、UID 和 TL，不显示专业标签。

### v0.6.0：节点与主题

目标：让用户可以浏览节点并发布、查看、编辑主题。

开发内容：

- 建立 Node、Topic、统一 Post、Revision 及必要统计字段；position=1 的 Post 作为首帖。
- 实现节点列表、节点详情、排序、归档和可见性。
- 实现主题创建、草稿、预览、发布、编辑和软删除。
- 实现首页主题流、节点主题流、游标分页和稳定排序。
- 实现置顶、精华、关闭状态；热门先保留计算接口。
- 实现标题、正文、发帖频率和链接数量规则。
- 实现作者、节点版主、全局版主、管理员的服务端授权骨架。
- 实现主题编辑历史和管理操作审计。

验收门槛：

- 每个已发布主题必须属于有效节点。
- 主题列表符合静态稿：节点在第二行灰色标识，状态位于标题右侧，不显示标签。
- 普通用户不能修改他人主题或管理状态。
- 删除与恢复不会破坏主题 ID、修订和审计关系。

### v0.7.0：回复、编辑器和附件

目标：完成社区讨论闭环。

开发内容：

- 实现 Markdown 编辑器、预览、草稿自动保存和安全渲染。
- 实现回复、楼层号、引用、编辑、软删除和修订历史。
- 实现 `@username` 提及解析，但通知在 v0.9.0 接通。
- 实现主题关闭后的回复限制和版主例外。
- 实现图片/附件上传、本地与 S3 Provider、引用检查和延迟回收。
- 实现内容长度、文件类型、MIME、危险链接和 SSRF 防护。
- 实现主题页分页或虚拟化策略并测试长主题性能。

验收门槛：

- 同一主题内楼层号稳定且唯一，删除回复不重排楼层。
- Markdown、引用和附件在前后台均经过安全清洗。
- 多人同时回复不会产生重复楼层或丢失计数。
- Worker 图片处理失败时原始内容和错误状态可追踪。

### v0.8.0：互动、搜索与内容发现

目标：让用户能够反馈、收藏、关注和找到内容。

开发内容：

- 实现点赞、取消点赞及数据库唯一约束。
- 实现主题收藏、用户关注、主题关注和阅读状态。
- 实现经过反滥用处理的浏览计数和异步统计聚合。
- 使用 PostgreSQL FTS/`pg_trgm` 实现标题、正文、用户和节点搜索。
- 实现最新、热门、精华等排序入口。
- 建立热门算法 v1：时间衰减、回复、独立参与者和反操纵限制。
- 实现个人收藏、关注和参与内容列表。
- 为搜索 Provider 保留接口，不增加强制第五个服务。

验收门槛：

- 重复请求不会产生重复点赞、收藏或关注记录。
- 搜索结果遵守节点可见性、删除和封禁规则。
- 热门排序可以解释主要信号，管理员不能直接伪造算法分数。
- Redis 清空后互动事实记录仍在 PostgreSQL 中。

### v0.9.0：通知、邮件和 Worker 完整链路

目标：把已有业务事件转换成可靠通知，并让 Worker 成为正式运行角色。

开发内容：

- 建立 Notification、NotificationPreference 和投递记录。
- 接通回复、提及、关注主题、管理动作等站内通知。
- 实现未读计数、通知列表、已读、全部已读和归档。
- 建立邮件模板、SMTP Provider 和测试邮件工具。
- 使用 Outbox 保证关键通知意图不会因入队故障丢失。
- 实现任务重试、幂等、死信查看和手工重放。
- 实现周期任务调度、分布式锁和 Worker 优雅停止。
- 在后台提供受权限控制的队列健康摘要。

验收门槛：

- 数据库提交后即使 Redis 临时不可用，恢复后仍能投递关键通知。
- 重试不会发送重复验证邮件、重复通知或重复副作用。
- 用户偏好能控制普通邮件，安全类邮件不被错误关闭。
- Worker 停止、恢复、升级和失败任务流程有集成测试。

### v0.10.0：举报、治理、角色和信任等级

目标：使社区具备基本治理和渐进式信任能力。

开发内容：

- 建立 Report、ModerationCase、ModerationAction、Sanction。
- 实现举报主题、回复和用户，支持聚合和防滥用。
- 实现警告、隐藏、恢复、关闭、节点禁言、全站禁言和封禁。
- 实现用户、节点版主、全局版主、管理员角色和权限矩阵。
- 实现 TL0-TL4 数据模型、规则版本、升级、降级、宽限期和历史记录。
- 实现账号时长、阅读、发帖、反馈和违规等计算指标。
- 实现信任重算 Worker、批处理进度和影响预估。
- 保证管理角色、TL、专业声誉和交易信用独立。

验收门槛：

- 每个管理动作有操作者、原因、目标、前后状态和请求 ID。
- TL3 不自动成为版主，管理员权限不依赖 TL。
- 制裁立即影响服务端授权，不能仅靠缓存或 UI。
- 信任规则调整可分批重算并能解释单个用户的等级来源。

### v0.11.0：管理后台与站点配置

目标：让管理员无需操作数据库即可完成日常运营。

开发内容：

- 实现后台仪表盘：注册、活跃、内容、举报、队列和系统状态。
- 实现用户搜索、详情、角色、会话、制裁和信任历史管理。
- 实现主题、回复、节点、举报案件和处置工作台。
- 实现站点名称、注册策略、发帖限制、信任规则和功能开关。
- 实现邮件、对象存储、OAuth Provider 的配置与连接测试。
- 实现 AuditLog 查询、过滤和受控导出。
- 实现高风险操作二次确认和管理员二次验证基础。
- 建立后台列表分页、筛选、批量操作权限和错误恢复。

验收门槛：

- 后台所有写操作经过统一服务端授权和审计。
- 敏感密钥不回显完整值，不进入浏览器日志。
- 普通用户和越权版主不能通过直接请求调用后台接口。
- 后台不只是数据库 CRUD，举报和处置流程可完整闭环。

### v0.12.0：安装、Docker、Actions 和运维

目标：把开发工程变成普通用户可以安装和维护的开源程序。

开发内容：

- 编写生产 Dockerfile，一个镜像提供 Web、Worker、setup/migrate/doctor。
- 编写默认四容器 Compose：Web、Worker、PostgreSQL、Redis。
- 实现 `nextbufctl` 薄封装，支持 init、start、stop、status、logs、doctor、backup、restore 和 upgrade，并保留等价标准 Compose 命令。
- 实现 setup 一次性初始化和受保护的首次管理员创建。
- 建立 GitHub Actions amd64/arm64 构建、测试、SBOM 和 GHCR 发布。
- 提供宝塔面板部署教程、反向代理、HTTPS 和升级步骤。
- 提供非 Docker 发布包、systemd 和 PM2 两进程示例。
- 实现数据库、附件、配置和密钥的备份/恢复脚本或操作指南。
- 建立版本检查、迁移预检、健康检查和 `doctor` 诊断输出。

验收门槛：

- 全新 Linux 环境按文档可以完成 Docker 和非 Docker 安装。
- setup 失败时 Web/Worker 不启动，并给出明确错误。
- 镜像在 amd64、arm64 上完成冒烟测试。
- 至少完成一次从备份恢复到空服务器的演练。
- 运行手册中的核心 `nextbufctl`、Docker 和非 Docker 命令均在全新环境验证通过。

### v0.13.0：公开 Beta 加固

目标：冻结 V1 功能范围，集中解决安全、性能、升级和体验问题。

开发内容：

- 完成安全威胁建模、依赖审计、CSP、CSRF、SSRF 和上传测试。
- 对首页、主题页、搜索、后台列表和 Worker 任务进行性能基准。
- 验证数据库索引、慢查询、连接池、Redis 内存和队列积压。
- 完成桌面、平板、移动端及键盘操作的可访问性检查。
- 建立从每个受支持 Beta 版本到当前版本的迁移测试。
- 完成日志脱敏、错误页、故障诊断和管理员告警。
- 邀请测试用户执行安装、注册、发帖、举报、升级和恢复。
- 清理临时功能开关、测试数据入口和调试接口。

验收门槛：

- 无未处理的已知严重安全问题和数据损坏问题。
- 核心 E2E、迁移、备份恢复和 Compose 测试稳定通过。
- 已知限制、最低资源和升级风险有文档。
- Beta 反馈按阻断、重要、增强分类并关闭阻断项。

### v1.0.0：首个稳定版本

目标：发布可长期运营的核心社区，而不是继续堆叠新功能。

发布内容：

- 冻结并发布 V1 数据库 Schema、配置合同和部署拓扑。
- 发布精确版本镜像、Compose、standalone 包、校验和和 SBOM。
- 发布安装、升级、备份、恢复、安全和故障排查文档。
- 建立漏洞报告渠道、支持版本政策和补丁发布流程。
- 完成 V1 发布说明、已知问题和 V1.1 之后的弃用规则。

发布门槛：

- 注册、登录、资料、发帖、回复、互动、通知、举报和后台主链路通过。
- Web/Worker 独立重启不导致已提交数据或关键任务丢失。
- 从最后一个 Beta 到 v1.0.0 的升级和恢复演练通过。
- 所有必需环境变量、卷、端口和 Provider 都与文档一致。

## 4. V1 稳定系列

### v1.0.1、v1.0.2 等补丁版本

只处理：

- 安全漏洞和依赖安全更新。
- 数据错误、崩溃、兼容性和升级问题。
- 不改变公开行为的性能优化。
- 文档、翻译、样式和无障碍缺陷。

补丁版本不得加入交易、插件等新领域，也不得要求破坏性配置迁移。

### v1.1.0：内容发现增强

- 节点关注和个性化首页筛选。
- 搜索语法、筛选、结果高亮和相关性调优。
- 热门算法 v2、趋势榜和反操纵监控。
- 精华集合、专题和管理员推荐，与算法热门分离。
- 用户内容列表和公开活动的隐私控制。

退出标准：新排序不泄露私密节点内容；旧 URL 和默认排序保持兼容。

### v1.2.0：订阅与摘要

- 每日/每周邮件摘要。
- 主题、节点和用户的通知级别。
- 免打扰时间、邮件频率和批量通知合并。
- Web Push 可行性验证并按 Provider 接入。
- 通知模板版本和投递统计。

退出标准：通知风暴有批处理和退订机制，安全邮件不受普通退订影响。

### v1.3.0：治理与反垃圾增强

- 举报队列优先级、合并、指派和内部备注。
- 新用户审核、链接域名规则、重复内容和行为风控。
- 节点级规则、版主范围和处置模板。
- 申诉流程、制裁到期和自动恢复。
- 信任规则模拟、异常升降检测和运营报表。

退出标准：风控规则可解释、可审计，误伤可恢复，不能形成不可见的永久封禁。

### v1.4.0：数据与运维工具

- 用户数据导出、注销和数据保留工具。
- 社区内容导入框架及首个受支持导入器。
- 后台任务诊断、重放、暂停和队列容量告警。
- 备份计划、远程备份 Provider 和恢复校验。
- 升级预检、迁移耗时估计和只读维护模式。

退出标准：导入、导出、删除和恢复均有审计及失败恢复策略。

### v1.5.0：集成与体验完善

- 更多 OAuth、SMTP、S3 兼容 Provider。
- Webhook 仅用于官方内部集成试验，不承诺完整公共 API。
- 管理后台效率、批量操作和移动端适配。
- 国际化基础、时区、日期和邮件模板改进。
- WCAG 目标检查和低性能设备优化。

退出标准：Provider 故障不会拖垮请求，配置诊断可以定位常见问题。

## 5. V2：开放平台与服务场景

### v2.0.0：公开 API 基础

- `/api/v1` 公开合同、OpenAPI 文档和错误规范。
- 个人 Token、应用 Token、Scope、过期、轮换和撤销。
- API 限流、请求审计、幂等键和分页规范。
- API 兼容政策、弃用头和版本生命周期。
- 对主题、回复、节点、用户公开资料提供首批只读/受控写入接口。

破坏性边界：正式冻结公开 API 语义，内部接口不得冒充公共 API。

### v2.1.0：Webhook 与官方 SDK

- 主题、回复、用户、举报等受控 Webhook 事件。
- 签名、重放防护、重试、死信和投递日志。
- TypeScript SDK 和最小调用示例。
- 应用注册、密钥轮换和管理员授权界面。
- Webhook payload 版本与兼容测试。

### v2.2.0：专业身份与作品

- 专业领域、作品集、外部验证和认证记录。
- 认证签发、过期、撤销、申诉和审计。
- 专业声誉模型，与 TL 和管理角色完全分离。
- 隐私、可见范围和防冒用机制。
- 专业资料搜索和展示组件。

### v2.3.0：服务信息与撮合

- 服务发布、分类、价格展示、交付说明和状态。
- 咨询/意向单，不处理真实资金。
- 服务收藏、评价资格占位和举报治理。
- 服务内容审核、垃圾信息限制和节点联动。
- 为 V4 订单系统保留稳定主体标识，不提前实现账务。

### v2.4.0：搜索与外部集成

- 可选 Meilisearch/OpenSearch Provider。
- 搜索索引重建、增量同步和降级回 PostgreSQL。
- 官方机器人/自动化账号的受控权限。
- 与 GitHub、状态页、主机监控等场景的官方集成。
- API 和 Webhook 的开发者后台与用量统计。

V2 完成标准：第三方可以在不访问数据库和内部 Prisma Model 的情况下完成稳定集成。

## 6. V3：插件平台

### v3.0.0：插件协议与生命周期

- Plugin Manifest、ID、版本、NextBuf 兼容范围和能力声明。
- 安装、启用、停用、升级、回滚和卸载状态机。
- 插件来源、签名、校验和及管理员风险确认。
- 插件配置 Schema、秘密字段和审计。
- 插件故障不能阻止核心应用启动的恢复模式。

### v3.1.0：服务端扩展

- 稳定领域事件订阅和版本化 payload。
- 邮件、存储、搜索、OAuth 等 Provider 插件接口。
- 插件队列任务命名空间、并发和资源限制。
- 数据库迁移命名空间、执行顺序和失败回退。
- 明确插件是受信任服务端代码，不虚假承诺完整沙箱。

### v3.2.0：界面与后台扩展

- 受控前台插槽、菜单、主题页动作和用户资料扩展。
- 管理后台导航、设置页和数据视图扩展。
- 组件 API、样式 token 和无障碍要求。
- 禁止插件直接覆盖核心文件或依赖不稳定 DOM 选择器。
- 插件和主题不得删除、隐藏或替换许可证要求的 NextBuf 页脚署名。
- 插件前端资源的 CSP、缓存和失败隔离。

### v3.3.0：插件开发工具

- 插件脚手架、类型包、测试工具和本地开发模式。
- 兼容性检查、Manifest 校验和迁移测试。
- 示例插件：一个 Provider、一个 UI 扩展、一个事件消费者。
- 插件诊断日志、性能统计和安全扫描入口。

### v3.4.0：插件目录

- 插件元数据目录、版本索引和下载校验。
- 作者验证、审核状态、兼容矩阵和安全通告。
- 管理员查看更新、变更权限和风险。
- 第一阶段只提供目录与分发，不急于实现商业结算。

V3 完成标准：核心升级不会静默破坏已声明兼容的插件；不兼容插件能被检测、阻止和恢复。

## 7. V4：订单、支付与争议

V4 必须在法务、税务、退款、隐私和地区可用性评审后才能进入 Committed。

### v4.0.0：订单与账务基础

- 报价、订单、交付、取消和完成状态机。
- 不可变业务流水、幂等操作和金额精度规范。
- 买卖双方身份、权限、证据和审计。
- 订单通知、超时任务和后台工作台。

### v4.1.0：支付 Provider

- 支付意图、回调签名、幂等和对账。
- Provider 适配层，不在核心散落渠道判断。
- 支付失败、超时、重复回调和状态修复。
- 密钥轮换、环境隔离和安全审计。

### v4.2.0：退款与争议

- 退款、部分退款、申诉、证据和裁决流程。
- 争议权限、时限、通知和审计。
- 交易信用计算，与社区 TL 分离。
- 防刷单、关联账号和评价资格控制。

### v4.3.0：商家与运营

- 服务方设置、订单统计和受控导出。
- 发票/税务接口按地区做 Provider，不在核心假设统一规则。
- 风控队列、人工复核和异常交易告警。
- 账务对账与灾难恢复演练。

## 8. V5：主题与扩展生态

### v5.0.0：主题系统

- 设计 token、模板插槽和主题 Manifest。
- 主题预览、启用、回退和安全模式。
- 核心语义 HTML 与主题表现分离。
- 主题兼容检测和无障碍最低标准。

### v5.1.0：扩展市场

- 插件、主题和 Provider 的统一目录。
- 版本、依赖、兼容范围和安全更新提示。
- 安装来源策略、镜像缓存和离线安装包。
- 评分只反映扩展体验，不与用户 TL 混合。

### v5.2.0：商业扩展能力

- 付费扩展许可证和更新授权接口的可选实现。
- 作者收益、退款和税务必须与 V4 账务体系对接。
- 开源扩展和私有扩展均保留可离线部署能力。
- 核心社区能力不能被强制依赖市场服务。

### v5.3.0：迁移与生态工具

- Discourse、NodeSeek 类数据结构的合规导入工具框架。
- 插件 API 迁移助手和弃用扫描。
- 主题视觉回归测试和多版本兼容测试。
- 社区维护者发布渠道和安全公告机制。

## 9. V6：高可用与规模化

### v6.0.0：多实例运行

- 多 Web、多 Worker 的正式支持矩阵。
- S3/CDN、外部 PostgreSQL、Redis 和搜索服务基线。
- 会话、缓存、限流和定时任务在多实例下的一致性。
- 无状态 Web、优雅滚动更新和健康摘除。

### v6.1.0：任务与数据扩展

- 队列按邮件、媒体、通知、索引、统计拆分并独立扩容。
- 大表归档、分区、在线索引和批量回填工具。
- 热点计数、缓存击穿和队列背压治理。
- 容量报告和资源建议自动化。

### v6.2.0：可观测性

- OpenTelemetry Trace、Metrics、Logs 标准输出。
- SLO、错误预算、队列延迟和核心业务指标。
- 跨 Web、Outbox、队列、Worker、Provider 的关联追踪。
- 隐私脱敏和采样配置。

### v6.3.0：灾难恢复

- 时间点恢复、对象存储版本和密钥恢复流程。
- 跨区域备份、恢复演练和自动校验。
- 只读降级、队列暂停和 Provider 隔离。
- 发布回滚和数据库 expand/contract 自动检查。

## 10. V7：组织与多社区

### v7.0.0：组织空间

- 组织、成员、角色、专属节点和内容边界。
- 组织邀请、审核、退出和所有权转移。
- 组织配置不能突破实例级安全策略。

### v7.1.0：自治社区空间

- 独立品牌、规则、版主范围和内容入口。
- 公共账号体系下的空间级身份和权限。
- 空间级审计、导出和配额。

### v7.2.0：多租户评审

- 评估共享 Schema、独立 Schema 或独立数据库。
- 明确数据隔离、备份恢复、计费和插件边界。
- 未通过隔离安全测试前，不宣称支持不可信租户 SaaS。

## 11. V8：联邦与数据可携带

### v8.0.0：可携带数据

- 标准化用户导出、内容导出和媒体清单。
- 可验证导入、ID 映射和来源记录。
- 用户名、身份和内容归属冲突处理。

### v8.1.0：跨实例订阅

- 只读订阅、签名事件和失败重放的实验协议。
- 实例信任、屏蔽、限流和内容删除传播。
- 不允许远程实例直接获得本地管理权限。

### v8.2.0：联邦发布

- 在完成协议、安全和治理验证后支持受控跨社区互动。
- 处理编辑、删除、封禁、媒体、举报和实例下线。
- 提供完全关闭联邦的部署选项。

V8 是研究性最强的系列，只有明确用户需求和互操作目标后才冻结协议。

## 12. V9：AI 原生社区能力

### v9.0.0：AI Provider 与权限

- 多模型 Provider、密钥、配额、成本和审计。
- 用户同意、隐私范围和内容发送边界。
- AI 输出明确标识，不能伪装成人类管理决定。

### v9.1.0：搜索与摘要

- 主题摘要、长讨论要点和语义搜索。
- 引用原帖位置，降低无来源生成内容。
- 支持关闭、重新生成和报告错误摘要。

### v9.2.0：写作辅助

- 标题建议、格式整理、重复主题提醒和翻译辅助。
- 默认由用户确认后发布，AI 不替用户自动发言。
- 保留必要审计但不保存多余敏感提示词。

### v9.3.0：治理辅助

- 举报聚类、垃圾内容候选和版主摘要。
- AI 只提供建议，高风险封禁和删除需要规则或人工确认。
- 持续评估误伤、偏见、绕过和成本。

## 13. V10：长期支持与平台成熟

### v10.0.0：LTS 基线

- 定义 LTS 支持周期、安全修复和升级窗口。
- 提供跨主要版本的迁移工具和兼容检查。
- 稳定核心 API、插件 API、数据导出格式和部署合同。
- 淘汰历史实验接口并提供完整迁移路径。

### v10.1.0：生态治理

- 插件/主题安全响应、撤回和维护者接管机制。
- 公开兼容测试、质量等级和供应链证明。
- 社区提案、ADR 和弃用流程标准化。

### v10.2.0：部署与迁移自动化

- 安装、升级、备份、恢复和迁移的统一管理工具。
- 从早期 NextBuf 版本和主要社区程序导入的验证流程。
- 自动生成环境诊断包，默认完成隐私脱敏。

### v10.3.0：平台稳定性

- 长期容量测试、故障注入和恢复演练。
- 稳定的多实例、插件、交易、组织和 AI 能力组合测试。
- 明确核心功能、官方扩展和社区扩展的维护责任。

V10 不表示产品开发结束，而表示 NextBuf 从“功能快速建设期”进入“长期兼容和生态治理期”。

## 14. 每个版本的统一开发流程

### 14.1 版本启动

1. 确认版本目标和不做事项。
2. 将功能拆为 Epic、用户故事和技术任务。
3. 确认数据库、权限、API、Worker 和部署影响。
4. 为高风险决策创建 ADR。
5. 定义迁移、回滚和观测指标。

### 14.2 开发顺序

单个功能按以下顺序推进：

```text
领域规则 -> 数据约束/迁移 -> 应用服务 -> 权限
-> Worker/事件 -> API/Server Action -> UI -> 测试 -> 文档
```

可以并行开发，但不能在领域和授权尚未定义时只完成页面外观后宣称功能完成。

### 14.3 发布候选

- 功能冻结，只接受该版本范围内的修复。
- 在旧版本数据副本上执行迁移。
- 使用最终镜像和 Compose 完成安装、升级、备份、恢复。
- 执行安全、性能、E2E 和多架构冒烟测试。
- 更新变更日志、已知问题和回滚限制。

### 14.4 发布后

- 观察错误率、数据库、队列、邮件和关键业务指标。
- 对严重问题发布补丁，不在紧急补丁中夹带新功能。
- 记录实际迁移时间、资源消耗和用户反馈。
- 将遗留问题进入下一版本，不静默扩大当前版本范围。

## 15. 单个版本的完成定义

每个版本发布前必须满足：

- 所有标记为 Must 的功能完成，Should 项可以明确延期。
- 数据库迁移从受支持前一版本验证成功。
- 权限拒绝路径与正常路径都有测试。
- Worker 任务具备幂等、重试和失败处理。
- 桌面、移动和键盘操作完成检查。
- 部署配置、环境变量、备份和升级文档同步更新。
- 无未说明的严重安全、数据损坏或升级阻断问题。
- 版本标签、镜像、源码、SBOM 和发布说明相互对应。

## 16. 如何调整本计划

可以调整版本顺序，但必须遵守依赖关系。例如：

- 用户资料依赖身份系统，不能先公开用户页再补权限。
- 通知依赖主题、回复和 Outbox，不能只做前端红点。
- 管理后台依赖治理用例，不能先生成全表 CRUD。
- 插件依赖稳定事件和 Provider，不能在核心模型仍频繁变化时冻结 API。
- 支付依赖订单、账务、幂等和法务评审，不能从一个支付按钮开始。

调整步骤：

1. 在本文件修改版本范围和依赖。
2. 在决策台账记录重大变化。
3. 若影响架构、数据库或部署，新增或替代 ADR。
4. 更新 Issue/Epic，不让文档与实际开发计划长期分叉。
